# Closed Doors

## Scope

This document records examined candidates that the owner deliberately leaves
unchanged. It is not an active work register.

| Date | Verdict | Candidate | Premise | Evidence | Carrying commit |
| --- | --- | --- | --- | --- | --- |
| 2026-08-17 | Keep | The golden ioc-runner consumer's engineer home (`/home/vmadmin`, mode 0700) blocks the non-root `ioc-srv` service account from traversing to the source-mode runner binary. Investigated here; cause is not in cloud-provision. | The 0700 home is the correct OS security default. The install and operational path uses `/usr/local/bin/ioc-runner` (world-readable) and never traverses the home, so the release gate is unaffected; only source-mode system tests cross the home. The coupling originates in where the runner source is placed, not in cloud-provision, so loosening the cloud-init home mode to 0711 was excluded by the owner. | Source-mode `system-lifecycle` S22 and S27 in `epics-ioc-runner` `tests/test-system-lifecycle.bash` fail only in source mode on fresh rocky8 and debian13 consumers. Root cause is `ansible-provision` `inventory/group_vars/all.yml` `path_ioc_runner_src`, which places the source under the 0700 home; `path_ioc_runner_bin` is `/usr/local/bin/ioc-runner`. cloud-init `templates/user-data.*` create `vmadmin` without setting a home mode, so it inherits the OS default 0700. Tracking transferred to `ansible-provision` M7 / jeonghanlee/ansible-provision#15. Resolution (owner option 3, keep the 0700 home and document the system-mode prerequisite): `ansible-provision` M7 Complete in `docs/milestone-a519802.md`, commit `0eefd11`, `#15` CLOSED; `epics-ioc-runner` documents the prerequisite in `gate/RUNBOOK.md` "System mode and the engineer home" and `docs/INSTALL.md` 4.1, commit `fdb9ff7`, `#147` closed with register sync `2862dfe`. cloud-provision keeps its cloud-init 0700 default unchanged (this door's carrying commit `2b77a97`, closure commit `0fb0afa`). | 2b77a97 |
| 2026-08-22 | Keep | The `docs/IMAGE_WORKFLOW.md` operator definition names `epics_env_build` as the P_epics-build role without naming pkg_automation, the external repository that supplies its EPICS development package list. | The operator definition is normative and records what each operator is, not where its pieces come from; source provenance is kept out of it by owner direction. Which repository owns the P_epics-build package list is an open definition decision recorded in that document's Open decisions section and, after the section is removed, in the work register. | `ansible-provision` `roles/epics_env_build/tasks/main.yml` clones and runs `pkg_automation.bash`; `pkg_automation` holds per-OS `common`, `epics`, and `extra` lists for every defined vacuum. Third-person review of the rewritten section raised the omission as info; the owner chose to keep the table as written. | 0569e2e |
| 2026-08-26 | Keep | The cloud-init `packages:` block is parsed in two places that must agree — the proxy-merge stripper in `bin/create_vm.bash` and the parity guard in `tests/check-package-parity.bash` — which could be generalized into one shared block-parser. | Generalize was declined. The two consumers want different outputs from the block (the stripper needs the line boundaries to drop the block; the guard needs the package names to check membership), the stripper is interwoven in the single-pass proxy-merge loop with strict post-conditions so refactoring it is risky, and the M5 guard's scope excludes create_vm changes. The latent seam is instead closed by Replace: the guard's block boundary is aligned to the stripper's, and reciprocal comments in both files record the coupling. Revisit Generalize only if a third consumer of the block appears or the two begin to diverge. | The stripper ends the block at the first line not starting with whitespace, a blank line included (`bin/create_vm.bash`); the guard's `extract_template_packages` now uses the same rule (`!/^[[:space:]]/`, `tests/check-package-parity.bash`) instead of ending only at a non-whitespace line. Observed agreement on an intra-block blank: the guard reads only the pre-blank entries, matching what the stripper drops; T1-T4 green after the change. conceptual-integrity sweep 2026-08-26. | 9630a1f |
| 2026-09-08 | Keep | Delivering an `/etc/tmpfiles.d/cloud-init.conf` override (`d /run/cloud-init 0755 root root - -`) from the rocky cloud-init templates so an in-place cloud-init upgrade keeps `/run/cloud-init` world-readable. | The rebuilt rocky cloud-init ships a tmpfiles rule setting the directory to 0700, but cloud-init resets it to 0755 at every boot, so the 0700 state exists only between an in-place upgrade and the next reboot; in this repository only the epics_build `dnf update` (ansible-provision, epics_dev species) performs that upgrade. A cloud-provision template `runcmd` cannot deliver the override safely: the proxy ADR requires the privileged apply first in `runcmd`, and because cloud-init runs `runcmd` as one `set -e`-less script whose exit status is its last line (D018), any command after apply masks an apply failure. The rocky templates carry no locale self-check to be that meaningful last line, so a rocky `runcmd` would move apply out of last position; a `bootcmd` avoids `runcmd` but still edits the five source templates the ADR keeps unchanged. The operator impact is already removed by M9 (the runbook hint reads status under `sudo`). Following the 2026-08-17 precedent, a guest-config problem triggered by an ansible-provision role is fixed in that role, not in cloud-provision templates, so cloud-provision keeps its rocky templates unchanged. | ADR-20260820 places the privileged apply first in `runcmd` and keeps the five source templates unchanged; D018 requires the self-check to stay the last `runcmd` entry because the flattened script's exit status is its last line. The rocky8 and rocky10 templates have no `runcmd` (grep count 0). Mechanism traced 2026-09-07 on the rocky8 epics-dev VM (0755 at boot, 0700 after `systemd-tmpfiles --create`). Operator path fixed in M9, commit `bd3a6e2`. Precedent: this document's 2026-08-17 row. The fix is routed to and landed in ansible-provision (the epics_build role, the LAB-ANSIBLE session's domain): the rocky branch of `roles/epics_build` writes `/etc/tmpfiles.d/cloud-init.conf` (`d /run/cloud-init 0755 root root - -`) after the dnf update and applies it with `systemd-tmpfiles --create`, shadowing the vendor rule (rocky family only); tracked and closed as jeonghanlee/ansible-provision#24 (commit `75cc487`), verified live on a rocky10 epics-dev guest on 2026-09-09. | `8bc52d6` |
| 2026-09-11 | Remove | Carrying `linux-source` (the Debian/Ubuntu full kernel-source package) in the shared EPICS build set (`configure/epics-packages`) on the debian12, ubuntu24, and ubuntu26 lines. | `linux-source` is an ethercat/rtbase kernel-module build dependency, not a general EPICS one, so the shared set that both EPICS acquisition paths install should not carry it. It was a verbatim carry on only three of the six OS lines (never debian13/rocky8/rocky10), a stray inconsistency, and pulled a ~193 MB download that slowed ubuntu26 provisioning. The ethercat/rtbase kernel-module build is owned by the rtbase species assembly (ansible-provision), applied through `bin/bake_ethercat_image.bash` (its own build toolchain and kernel headers), not by this set; owner-confirmed ethercat-only. | `linux-source` appeared only on `configure/epics-packages` lines debian12/ubuntu24/ubuntu26 and in no other package list in the repository; introduced verbatim in `c389cac`; absent from debian13/rocky8/rocky10 (grep count 0). `bin/bake_ethercat_image.bash` applies ansible-provision rtbase assembly (build toolchain, kernel headers, dkms, RT kernel) for the `debian13-ethercat` variant, and `debian13-ethercat` already builds with no `linux-source` in its line, so removal does not affect ethercat. `tests/check-epics-packages.bash` green after removal (debian12 42, ubuntu24 39, ubuntu26 39). The full six-OS EPICS-env 1.4.0 gz build passed without it (reported by the EPICS-env session). Requested cross-session and owner-confirmed 2026-09-11; ansible-provision mirrors the set as `epics_os_packages` and is asked to drop it there too. | a24d07f |
